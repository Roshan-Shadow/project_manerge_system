# 项目管理系统 · 作战驾驶舱 Wiki

## 目录

- [系统概述](#系统概述)
- [快速开始](#快速开始)
- [功能模块](#功能模块)
  - [主页](#主页)
  - [仪表盘](#仪表盘)
  - [甘特图](#甘特图)
  - [计划](#计划)
  - [需求](#需求)
  - [缺陷](#缺陷)
  - [模板中心](#模板中心)
- [主题系统](#主题系统)
- [项目仓库](#项目仓库)
- [数据模型](#数据模型)
- [技术架构](#技术架构)
- [常见问题](#常见问题)

---

## 系统概述

项目管理系统·作战驾驶舱是一款基于 Electron 的桌面应用，采用 Vanilla TypeScript 开发，提供完整的项目管理功能闭环。

### 核心特性

- **七大功能模块**：主页、仪表盘、甘特图、计划、需求、缺陷、模板中心
- **六套可切换主题**：熔金战情、深海蓝讯、翡翠方舟、曙光白域、暮霭柔光、晴空素笺
- **内存数据仓库**：无需后端即可完整演示增删改查闭环
- **文件系统持久化**：项目数据以 JSON 快照形式保存在本地仓库

---

## 快速开始

### 环境要求

- Node.js 16+
- npm 8+

### 安装与运行

```bash
# 克隆仓库
git clone https://github.com/Roshan-Shadow/project_manerge_system.git
cd project_manerge_system

# 安装依赖
npm install

# 启动应用
npm start
```

### 其他命令

| 命令 | 说明 |
|------|------|
| `npm run build` | 编译 TypeScript |
| `npm run serve:web` | 浏览器预览模式 |
| `npm run dist` | 打包 Windows 安装包 |
| `npm run dist:dir` | 仅打包免安装目录 |

---

## 功能模块

### 主页

主页展示所有项目的概览卡片墙，支持以下功能：

- **项目卡片**：显示状态、周期、完成率、任务数、延期数、下一任务截止
- **排序方式**：
  - 按创建时间排序
  - 按下一任务截止排序（无未完成任务排末尾）
  - 按最终完成时间排序
- **快捷操作**：进入项目、打开仓库、导出快照

### 仪表盘

仪表盘提供项目数据的实时聚合视图：

| 组件 | 说明 |
|------|------|
| 指标卡片 | 可展开查看明细，包含任务数、需求数、缺陷数、完成率 |
| 环形完成度 | 可视化项目整体进度 |
| 计划 vs 实际 | 对比计划工期与实际用时 |
| 折线图 | 近30天完成任务/新增缺陷趋势 |
| 优先级看板 | 按优先级分布的任务/需求数量 |
| 热力地图 | 成员×阶段负载分布 |
| 风险告警日历 | 红色=已延期，金色=3天内到期 |

### 甘特图

甘特图提供项目时间线的可视化视图：

- **视图切换**：日视图、周视图、月视图
- **筛选功能**：按负责人、状态筛选
- **交互操作**：
  - 拖拽任务条整体平移排期
  - 拖拽右缘调整工期
  - 点击任务条打开编辑
- **今日金线**：标识当前日期位置

### 计划

计划模块是项目管理的核心，支持阶段和任务的完整管理：

#### 阶段管理

- 新增阶段
- 编辑阶段（名称、提示、交付物配置）
- 删除阶段
- 文件夹跳转

#### 任务管理

- 新增任务
- 编辑任务（标题、负责人、日期、优先级、进度等）
- 状态流转：待开始 → 进行中 → 已完成
- 用时统计：首次进入"进行中"到"已完成"的时长
- 交付物管理：添加、确认接收、删除
- 文件提交：复制文件到仓库并登记为交付物
- 批量操作：多选删除

#### 编辑计划

点击"编辑计划"按钮打开集成编辑界面：

- 阶段列表：增删改阶段
- 任务列表：增删改任务
- 交付物管理：每个任务的交付物配置
- 阶段提示：为阶段添加说明文字

#### 整体导入

支持通过 JSON 文件批量导入项目计划：

**导入范式**：

```json
{
  "phases": [
    {
      "name": "阶段名称",
      "tip": "阶段提示（可选）",
      "tasks": ["任务1", "任务2"],
      "taskDeliverables": {
        "0": [{ "name": "交付物名称", "note": "说明（可选）" }]
      }
    }
  ]
}
```

### 需求

需求模块支持需求的全生命周期管理：

- 新增需求
- 关联任务（需求拆解）
- 状态流转
- 关联任务数查看

### 缺陷

缺陷模块支持缺陷的跟踪与管理：

- 登记缺陷
- 严重程度分级：致命(1天)、严重(2天)、一般(4天)、轻微(7天)
- SLA 超期提醒：超期任务红色脉冲提示

### 模板中心

模板中心提供项目模板的管理功能：

#### 系统模板

4个内置模板：

1. **软件研发迭代**：需求评审 → 开发 → 测试 → 上线
2. **产品发布**：策划 → 开发 → 预发布 → 正式发布
3. **日常运营项目**：需求收集 → 执行 → 验收 → 总结
4. **论文撰写**：选题与开题 → 研究与实验 → 论文撰写 → 修改与定稿

#### 自定义模板

- 从项目另存为模板
- Monaco JSON 编辑器编辑结构
- 复制、重命名、删除

---

## 主题系统

系统提供六套主题，支持实时切换：

### 深色主题

| 主题 | 配色方案 | 说明 |
|------|----------|------|
| 熔金战情 | 橙红 × 暖金 | 默认主题，火焰流光效果 |
| 深海蓝讯 | 冰蓝 × 青辉 | 冰冷科技感 |
| 翡翠方舟 | 翠绿 × 薄荷 | 清新自然感 |

### 浅色主题

| 主题 | 配色方案 | 说明 |
|------|----------|------|
| 曙光白域 | 米白基底 × 暖橙点缀 | 经典浅色 |
| 暮霭柔光 | 薰衣草灰 × 玫瑰金 | 柔和优雅 |
| 晴空素笺 | 天蓝 × 云白 | 清爽明快 |

### 主题切换

点击头部【主题】按钮，选择目标主题即可即时生效。

主题偏好保存在 localStorage，刷新/重启后保持。

---

## 项目仓库

### 自动仓库

新建项目后自动在 `<工作目录>/repos/<项目目录>` 创建仓库。

### 目录结构

```
repos/
└── 项目名_短ID/
    ├── project.json          # 项目状态快照
    ├── 01_需求评审/          # 阶段文件夹
    │   ├── 用户调研/         # 任务文件夹
    │   └── 需求文档/
    ├── 02_开发/
    │   ├── 前端开发/
    │   └── 后端开发/
    └── ...
```

### project.json

仓库根目录的 `project.json` 记录项目完整状态：

- 项目基本信息
- 阶段列表
- 任务列表
- 需求列表
- 缺陷列表
- 仓库目录结构
- 生成时间

### 导入导出

- **导出**：仪表盘概况卡【⇩ 导出快照】→ 另存单个 `.json` 文件
- **导入**：头部【⇪ 导入项目】→ 选择迁移文件 → 完整恢复

---

## 数据模型

### Phase（阶段）

```typescript
interface Phase {
  id: string;
  projectId: string;
  name: string;
  order: number;
  tip?: string;           // 阶段提示
}
```

### Task（任务）

```typescript
interface Task {
  id: string;
  projectId: string;
  phaseId: string;
  title: string;
  owner: string;
  startDate: string;
  endDate: string;
  hours: number;
  progress: number;
  status: TaskStatus;     // '待开始' | '进行中' | '已完成'
  priority: Priority;     // 'P0' | 'P1' | 'P2'
  desc: string;
  tip?: string;           // 任务提示
  deliverables: Deliverable[];
  completedAt: string;
}
```

### Deliverable（交付物）

```typescript
interface Deliverable {
  id: string;
  name: string;
  note: string;
  time: string;
  accepted: boolean;
}
```

---

## 技术架构

### 进程模型

```
Electron App
├── Main Process (src/main/)
│   ├── 内存数组仓库
│   └── IPC Handler
├── Preload (src/preload/)
│   └── contextBridge 桥
└── Renderer Process (src/renderer/)
    ├── core/
    │   ├── app.ts          # 应用编排
    │   ├── dom.ts          # DOM操作/表单校验
    │   ├── effects.ts      # 粒子/网格特效
    │   ├── state.ts        # 状态管理
    │   ├── store.ts        # 仓库API
    │   └── theme.ts        # 主题系统
    └── modules/
        ├── dashboard.ts    # 仪表盘
        ├── gantt.ts        # 甘特图
        ├── plan.ts         # 计划
        ├── requirement.ts  # 需求
        ├── bug.ts          # 缺陷
        └── template.ts     # 模板中心
```

### 编译目标

- **主进程/预加载**：CommonJS → `dist/cjs/`
- **渲染进程**：ESM → `dist/renderer/`

### 关键依赖

| 依赖 | 用途 |
|------|------|
| electron | 桌面应用框架 |
| monaco-editor | JSON编辑器 |
| electron-builder | 打包工具 |

---

## 常见问题

### Q: 如何切换主题？

点击头部右侧的【主题】按钮，在下拉菜单中选择目标主题即可。

### Q: 数据会丢失吗？

- 浏览器预览模式：数据保存在内存，刷新后清空
- 桌面应用模式：数据保存在 `<工作目录>/repos/` 目录

### Q: 如何备份项目？

1. 打开仪表盘
2. 点击概况卡的【⇩ 导出快照】
3. 保存 `.json` 文件

### Q: 如何恢复项目？

1. 点击头部【⇪ 导入项目】
2. 选择之前导出的 `.json` 文件
3. 项目及全部子数据完整恢复

### Q: 模板编辑后会保存吗？

- 系统模板：仅在本次会话内可编辑，重启后恢复默认
- 自定义模板：以 JSON 文件形式持久化保存

### Q: 如何打包分发？

```bash
npm run dist
```

产物在 `release/` 目录：
- `PMS-Cockpit-Setup-1.0.0.exe`：安装版
- `PMS-Cockpit-Portable-1.0.0.exe`：便携版

---

## 更新日志

### v1.1.0 (2026-08-30)

- 新增整体导入功能
- 新增编辑计划集成界面
- 优化计划页面交互
- 项目进度100%自动标记完成
- 新增暮霭柔光、晴空素笺主题
- 优化深色主题背景色彩

### v1.0.0 (2026-08-25)

- 初始版本发布
- 七大功能模块
- 四套主题
- 项目仓库管理
